# Specification 39: Documentation Publishing Pipeline

## 1. Overview
The Sovereign Exosystem utilizes a CLI-driven publishing pipeline to convert raw Markdown from the monorepo into high-fidelity web surfaces and portable documents. This ensures that the knowledge base remains both sovereign and accessible across different environments.

This specification defines the use of **MkDocs Material** for site generation and **Pandoc** for multi-format document export.

---

## 2. Web Generation: MkDocs Material

The monorepo serves as the primary documentation hub. For web-based viewing, we utilize MkDocs with the Material theme to provide a responsive, searchable interface.

### 2.1 Technology Stack
- **Generator**: MkDocs (Python-based).
- **Theme**: Material for MkDocs.
- **Styling**: ExoTheme CSS variables integrated via `extra_css`.

### 2.2 Directory Structure
The pipeline follows a dedicated public publishing path:
- **Source**: `docs/public/` — Contains the Markdown files intended for public site generation.
- **Output**: `docs/public/site/` — The static HTML site generated by `mkdocs build`.

---

## 3. Multi-Format Export: Pandoc

To facilitate portability and offline access, the document viewer UI includes a download dropdown powered by **Pandoc**. This allows users to export individual documents in their preferred format.

### 3.1 Supported Export Formats
The UI provides a selection of professional formats:
- **Plain Text (`.txt`)**: For maximum compatibility and low-overhead reading.
- **Markdown (`.md`)**: To preserve the raw sovereign source format.
- **PDF (`.pdf`)**: For high-fidelity, printable deliverables (rendered via `weasyprint`).
- **DOCX (`.docx`)**: For integration with legacy word processing environments.
- **ePub (`.epub`)**: For optimized reading on mobile devices and e-readers.

### 3.2 Export Workflow
When a user selects a format from the dropdown:
1. The app triggers a Pandoc conversion on the server (Conscia node).
2. Appropriate CSS or templates are applied to the export.
3. The resulting file is served to the user for download.

---

## 4. Visual Assets & Diagrams

The Exosystem favors high-fidelity, custom-generated imagery over text-based diagramming tools.
- **No Mermaid**: Mermaid.js or other text-to-diagram tools are not used.
- **Custom Graphics**: All technical diagrams are provided as image assets (PNG/SVG) generated specifically to illustrate the system's architecture and flows.

---

## 5. Testing & Deployment

- **Deployment Testing**: `exocracy.org` is currently utilized as a testing ground for these deployment pipelines.
- **Automation**: Build and conversion processes can be scripted via the Conscia API for enterprise-scale documentation management.

---

## 6. Related Documents
- [Spec 22: Application Triad Architecture](./22_application_triad_architecture.md)
- [Spec 38: Conscia Federation & Service Architecture](./38_conscia_federation_and_services.md)
- [Spec 40: P2P SDUI & Meta-Management](./40_p2p_sdui_and_metamanagement.md)
