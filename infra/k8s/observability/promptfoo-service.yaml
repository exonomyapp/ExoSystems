apiVersion: v1
kind: Service
metadata:
  name: promptfoo
  namespace: default
spec:
  type: NodePort
  selector:
    app: promptfoo
  ports:
    - name: ui
      port: 3000
      targetPort: 3000
      nodePort: 32300
